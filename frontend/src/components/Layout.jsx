
import React from 'react';

import Navbar from './Navbar';
import  Router  from "./Router";

const Layout = () => {
  return (
    <div>
        <Navbar />
        <Router/>
    </div>
  );
};

export default Layout;
